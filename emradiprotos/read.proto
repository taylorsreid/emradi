syntax = "proto3";

// Go package name
option go_package = "../emradiprotos"; 

message Read {
    bool multiple = 1;
    string collection = 2;
    string filter = 3;
    optional string sort = 4;
    optional int32 limit = 5;
    optional int32 offset = 6;
}