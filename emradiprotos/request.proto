syntax = "proto3";

// Go package name
option go_package = "../emradiprotos";

enum Verb {
    UNKNOWN = 0;
    CREATE = 1;
    READ = 2;
    UPDATE = 3;
    DELETE = 4;
}

message Request {
    string id = 1;
    Verb verb = 2;
    
}